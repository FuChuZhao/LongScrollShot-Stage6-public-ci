# LongScrollShot-Stage6-public-ci

公有仓库，仅用于跑 GitHub Actions 构建（源码在私有仓库，通过 Deploy Key 拉取）。

- Workflow：`.github/workflows/android.yml`
- Artifacts：`apks`（各 probe + `longscrollshot-release.apk` / `longscrollshot-hyperos15-release.apk`）
